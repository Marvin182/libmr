#include "version.h"

namespace mr {

const mr::util::VersionInfo version(${git_version_major}, ${git_version_minor}, ${git_version_revision}, ${git_version_build}, "${git_version_commit}", "${git_version_branch}", "${git_version_stage_name}", ${git_version_stage_number});

} // namespace mr